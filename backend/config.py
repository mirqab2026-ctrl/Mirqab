"""
الإعدادات المركزية الموحّدة — Mirqab Config
============================================
العتبة الموحّدة للوجه تُعرَّف هنا فقط (قرار تصميمي):
- لا تُخزَّن في قاعدة البيانات.
- لا يمكن تغييرها من الواجهة (أُلغي الوضع المخصص وسلايدرات العتبة).
- تُعاير عبر scripts/calibrate_face_threshold.py ثم تُثبَّت هنا.

ملاحظة: مع ArcFace (cosine similarity) العتبة مستقرة عبر الإضاءات
والزوايا المختلفة، بعكس dlib القديم، لذا قيمة واحدة تكفي فعلاً.
"""
from pathlib import Path

ROOT = Path(__file__).parent.parent

# ============== الوجه — العتبة الموحّدة ==============
# cosine similarity: التطابق يُقبل عندما similarity >= العتبة.
# مُعايَرة فعلياً (2026-06-06) على 82 شخصاً — التقرير: data/face_threshold_report.json
#   أزواج حقيقية: 0.921 ± 0.068 | أزواج دخيلة: 0.088 ± 0.138
#   عند 0.54: قبول صحيح 100%، قبول خاطئ 2.15% → اخترنا 0.58 بميل أمني.
# ملاحظة: إن فشل التطابق من الكاميرا الحية (إضاءة/زاوية أسوأ من صور
# التسجيل) جرّب خفضها تدريجياً حتى 0.50 — لا تنزل تحتها.
FACE_MATCH_THRESHOLD = 0.58

# ============== محرك الوجه ONNX (buffalo_s) ==============
FACE_MODELS_DIR = ROOT / "models" / "face"
FACE_DET_MODEL = FACE_MODELS_DIR / "det_500m.onnx"      # SCRFD-500MF (كشف + 5 معالم)
FACE_REC_MODEL = FACE_MODELS_DIR / "w600k_mbf.onnx"     # MobileFaceNet (تشفير 512-d)

# حجم إدخال الكاشف: 320 أسرع ~4× من 640 على CPU وكافٍ لوجه قريب من البوابة.
FACE_DET_SIZE = (320, 320)
# عتبة ثقة "كشف" الوجه (وجود وجه في الصورة، ليست عتبة التطابق)
FACE_DET_THRESHOLD = 0.50

# أبعاد التشفير حسب المحرك (للتمييز بين الترميزات القديمة والجديدة في DB)
EMBEDDING_DIM_ONNX = 512
EMBEDDING_DIM_DLIB = 128
